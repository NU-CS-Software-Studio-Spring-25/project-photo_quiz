<p style="color: green"><%= notice %></p>

<% content_for :name, "Courses" %>

<h1>Courses</h1>

<div class="courses-container">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
      <%= link_to "Show this course", course %>
    </p>
  <% end %>
</div>

<%= link_to "New course", new_course_path %>
