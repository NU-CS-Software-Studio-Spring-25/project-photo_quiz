<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>Photo Quiz (NU)</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUa1zYhlsU6pdt36U8Hft8z8sL6b1Z8n3e6e5e5e5e5e5e5e5e5e5e5e5" crossorigin="anonymous">
</head>

<body>
  <div class="container py-4">

    <div class="actions mb-4">
      <h4>Directory</h4>
      <input type="text" class="form-control rounded-pill mb-3" placeholder="Search students..." />
      <%= link_to 'New Student', new_student_path, class: "btn btn-nu me-2" %>
      <%= link_to 'Quiz', quizzes_path, class: "btn btn-nu" %> 
    </div>

    <!-- card list -->
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% @students&.each do |student| %>
        <div class="col">
          <div class="card h-100 shadow-sm">
            <% if student.profile_picture.attached? %>
              <%= image_tag url_for(student.profile_picture), class: "card-img-top", alt: "#{student.first_name}'s photo", style: "height: 200px; object-fit: cover;" %>
            <% else %>
              <div class="card-img-top d-flex justify-content-center align-items-center bg-light" style="height: 200px;">
                <span class="text-muted">No photo</span>
              </div>
            <% end %>

            <div class="card-body">
              <p class="course-code text-muted mb-1"><%= student.course %></p>
              <h6 style="font-size: 1.1rem; font-weight: 600;"><%= student.first_name %> <%= student.last_name %></h6>
              <div class="mt-3">
                <%= link_to 'Edit', edit_student_path(student), class: "btn btn-warning btn-sm me-2" %>
                <%= link_to 'Delete', confirm_destroy_student_path(student), class: "btn btn-primary btn-sm" %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</body>
</html>
