<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>Photo Quiz (NU)</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="./node_modules/bootstrap/dist/css/bootstrap.min.css"/>
  <style>

  </style>

</head>

<body>
  <div class="container py-4">
    <h2>Your Courses </h2>
    <div class="actions mb-4">
      <h4>Directory</h4>
      <input type="text" class="form-control rounded-pill" placeholder="Search courses..." />
      <%= link_to 'New Course', new_course_path, class: "btn btn-nu" %>
    </div>

    <!-- card list -->
    <div class="container">
        <h2> Your Courses </h2>
        <div class="row">
            <% current_user.courses.each do |course| %>
                <div class="col-md-4 mb-3">
                    <div class="card mb-3">
                        <div class="card-body">
                            <h5><%= course.name %></h5>
                            <%= link_to 'View Students', course_path(course), class: "btn btn-primary" %>
                        </div>
                    </div>
                </div>
            <% end %>
        </div>
        <%= link_to 'New Course', new_course_path, class: 'btn btn-success' %>
    </div>
    </div>

</body>
</html>