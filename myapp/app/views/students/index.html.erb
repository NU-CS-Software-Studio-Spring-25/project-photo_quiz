<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>Photo Quiz (NU)</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUa1zYhlsU6pdt36U8Hft8z8sL6b1Z8n3e6e5eF5e5e5e5e5e5e5e5e5e5e5" crossorigin="anonymous">

</head>

<body>
  <div class="container py-4">

    <div class="actions">
      <h4>Directory</h4>
      <input type="text"
             class="form-control rounded-pill"
             placeholder="Search students..." />
       <%= link_to 'New Student', new_student_path, class: "btn btn-nu" %>
       <%= link_to 'Quiz', quizzes_path, class: "btn btn-nu" %> 
    </div>

    <!-- card list -->
    <div class="cards-container">
      <% @student.each do |student| %>
        <div class="card card-large">
            <div class="card-img-top"></div>
            <div class="card-body">
            <p class="course-code"> <%= student.course %> </p>
            <h6> <%= student.first_name %> <%= student.last_name %></h6>
            <div class="mt-3">
              <%= link_to 'Edit', edit_student_path(student), class: "btn btn-warning btn-sm" %>
              <%= link_to 'Delete', confirm_destroy_student_path(student), class: "btn btn-primary btn-sm" %>

            </div>
            </div>
      </div>
    <% end %>

  </div>


</body>
</html>
